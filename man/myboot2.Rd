% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/myboot2.R
\name{myboot2}
\alias{myboot2}
\title{Bootstrap Confidence Interval}
\usage{
myboot2(iter = 10000, x, fun = "mean", alpha = 0.05, cx = 1.5, ...)
}
\arguments{
\item{iter}{Number of bootstrap iterations. Default is 10000.}

\item{x}{Numeric sample to bootstrap.}

\item{fun}{Statistic to compute (e.g. "mean", "sd", median, IQR).
Default is "mean".}

\item{alpha}{Significance level. Default is 0.05 for a 95\% CI.}

\item{cx}{Text size for plot labels. Default is 1.5.}

\item{...}{Extra arguments passed to \code{hist()}.}
}
\value{
Invisibly returns a list with the confidence interval, the statistic used,
        and the original data.
}
\description{
This function uses the bootstrap to estimate a confidence interval for a
statistic of interest. It resamples the data, computes the statistic for each
resample, and plots the bootstrap distribution with the CI and point estimate.
}
\examples{
\dontrun{
  set.seed(1)
  sam <- rnorm(20, mean = 10, sd = 4)
  myboot2(x = sam, fun = "mean")
}
}
